defaults:
  - setup
  - skill_setup
  - datamodule: calvin_ds
  - logger: wandb

logger:
  group: gmm-eval

# Dataset params:
train: false # false to load validation dataset
goal_centered: false # this must be false during evaluation

# Environment params
render: false
record: false

hydra:
  run:
    dir: ${log_dir}/gmm-eval-runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${log_dir}/gmm-eval-runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}
  job:
    config:
      override_dirname:
        exclude_keys:
          - log_dir