_target_: sac_gmm.datasets.calvin_dynsys.CALVINDynSysDataset
skill: ${skill}
train: ${train}
state_type: ${state_type}
demos_dir: ${demos_dir}
goal_centered: ${goal_centered}
dt: ${dt}
sampling_dt: ${sampling_dt}
normalized: ${normalized}
is_quaternion: ${is_quaternion}
